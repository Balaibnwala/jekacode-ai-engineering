# Notebook generators

Run from the course root if you need to rebuild **day1/day2/day3** notebooks:

```bash
python tools/generate_days_1_3.py
python tools/generate_days_4_12.py
```

Do not run the older `generate_weeks_*.py` files — they write the retired `01_learn` names.
